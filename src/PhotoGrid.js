import React from 'react';
import axios from 'axios';
import Photos from "./Photo";

export default function PhotoGrid( [limit ]) {




  return (
    <div className="photo-grid">
    <button onClick=[() => setType("")]>PhotoType</button>
    {card.map(photoUrl) =>
    return Photos key=}

    </div>


  )
}
